name = "sleepy-ud"
compatibility_date = "2024-12-01"
pages_build_output_dir = "public"

[vars]
JWT_SECRET = "change-this-in-production"

# D1 binding is set via Cloudflare Pages dashboard (Settings → Functions → D1 bindings)
